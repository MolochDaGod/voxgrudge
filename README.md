# VoxGrudge

Open-world voxel Grudge Studio title. Production: https://voxgrudge.vercel.app

## Character scale

- Default model normalize height: **2.0 m** (ORC / humanoid fleet SSOT) in `js/vox-assets.js`.

## Choose Survivor

- Class cards always paint via `ensureSurvivorsUI` grid fallback if the carousel fails.
